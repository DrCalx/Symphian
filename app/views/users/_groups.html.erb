<section>
    <h2>My Groups</h2>
    <% if current_user?(@user) %>
      <%= link_to "Create New Group", "/groups/new", id: "edit_button", class: "btn btn-xs btn-primary btn-edit" %>
    <% end %>
</section>
<section>
    <%= render current_user.groups.order(name: :asc) %>
</section>