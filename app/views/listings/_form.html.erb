<% form_for :listing, :html => {:role => "form", :class => "form-vertical"} do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, :class => "form-control" %>

	<%= f.submit "Post Listing" %>
<% end %>